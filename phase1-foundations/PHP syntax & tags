<!-- 
================================================
PHP SYNTAX - The rules that define how PHP code 
must be written
================================================
-->

<?php
// ============================================
// 1. PHP STATEMENTS - Every statement ends with semicolon
// ============================================

/*
Think of semicolons (;) like periods in English sentences
They tell PHP "this instruction ends here"
*/

// Correct - statements end with semicolon
echo "Hello World";
$name = "John";
$age = 25;
$result = 10 + 5;

// Incorrect - missing semicolon will cause error
// echo "Hello World"  // Parse error: syntax error

// Multiple statements on one line (not recommended but possible)
echo "Hi"; $city = "New York"; echo " there";

// Empty statement is valid (rarely used)
; // Does nothing

// ============================================
// 2. PHP VARIABLES - All variables start with $
// ============================================

/*
The $ symbol tells PHP "this is a variable name"
Like a flag marking a container for data.
*/

$first_name = "John";     // String variable
$age = 30;                // Integer variable
$price = 19.99;           // Float variable
$is_active = true;        // Boolean variable

// ============================================
// 3. CASE SENSITIVITY - Variables are case-sensitive
// ============================================

/*
Variables: YES case-sensitive ($name ≠ $Name)
Keywords/functions: NOT case-sensitive (echo = ECHO = EcHo)
*/

// Variables are case-sensitive - these are DIFFERENT variables
$name = "John";           // Variable with lowercase n
$Name = "Jane";           // Different variable with uppercase N
$NAME = "Bob";            // Another different variable

echo $name;  // Outputs: John
echo $Name;  // Outputs: Jane
echo $NAME;  // Outputs: Bob

// Keywords and function names are NOT case-sensitive
echo "Hello";             // Standard way
ECHO "Hello";             // Works but NOT recommended
EcHo "Hello";             // Works but extremely NOT recommended

// ============================================
// 4. COMMENTS - Documenting your code
// ============================================

/*
Comments = notes for humans, ignored by PHP
Think of them as sticky notes in your code
*/

// Single-line comment (C++ style)

# Single-line comment (Unix shell style)

/*
Multi-line comment (C style)
Can span multiple lines
Useful for longer explanations
*/

/**
 * PHPDoc style comment
 * Used for documentation
 * @param string $name The name to display
 * @return void
 */

// Comments can be used to disable code temporarily
// echo "This won't run";
/*
$test = "This block";
echo "is completely";
echo "commented out";
*/

// ============================================
// 5. WHITESPACE - Spaces, tabs, new lines
// ============================================

/*
PHP ignores extra whitespace
Use it to make code more readable
*/

// These all work the same:
echo "Hello";
echo      "Hello";        // Extra spaces ignored
echo "Hello" ;            // Space before semicolon
echo 
"Hello";                  // New line ignored

// Indentation improves readability
if ($condition) {
    echo "Indented code";     // Easier to read
    if ($subcondition) {
        echo "More indented"; // Shows structure
    }
}

// ============================================
// 6. PHP TAGS - Markers that tell server "this is PHP"
// ============================================

/*
PHP tags = the markers that tell the server 
"this is PHP code, process it"
*/

// -------------------------------------------------
// TYPE 1: STANDARD PHP TAGS (Recommended)
// -------------------------------------------------
<?php
// This is the most reliable and recommended tag
// Works on all servers
echo "Standard PHP tag";
?>

// -------------------------------------------------
// TYPE 2: SHORT TAGS (Not recommended)
// -------------------------------------------------
<? 
// This is short tag - requires short_open_tag enabled
// May not work on all servers
echo "Short tag"; 
?>

// -------------------------------------------------
// TYPE 3: ASP STYLE TAGS (Deprecated)
// -------------------------------------------------
<%
// ASP style - deprecated, avoid using
echo "ASP tag";
%>

// -------------------------------------------------
// TYPE 4: SCRIPT TAGS (Rarely used)
// -------------------------------------------------
<script language="php">
// HTML script tag style - not commonly used
echo "Script tag";
</script>

// ============================================
// KEY RULES FOR PHP TAGS
// ============================================

/*
1. Opening tag <?php is mandatory
2. Closing tag ?> is optional
3. If file is ONLY PHP, omit closing tag
4. Everything outside tags is treated as HTML
*/

// RULE 1: Opening tag is mandatory
<?php
// Code here must be valid PHP
$valid = true;
?>

// RULE 2: Closing tag is optional
<?php
echo "No closing tag needed";
// File can end here without ?>

// RULE 3: Pure PHP files should omit closing tag
<?php
// This entire file is PHP only
// No closing tag ?> - prevents accidental whitespace

// RULE 4: Mixing PHP and HTML
<html>
<body>
    <?php 
    $name = "John"; 
    ?>
    <h1>Welcome <?php echo $name; ?>!</h1>
    <?php
    // More PHP code
    echo "Footer";
    ?>
</body>
</html>

// ============================================
// PRACTICAL EXAMPLES - Mixing PHP and HTML
// ============================================

<!-- Example 1: PHP inside HTML -->
<!DOCTYPE html>
<html>
<head>
    <title>PHP Example</title>
</head>
<body>
    <?php
    $pageTitle = "My Website";
    $currentTime = date("H:i:s");
    ?>
    
    <h1><?php echo $pageTitle; ?></h1>
    
    <p>The current time is: <?php echo $currentTime; ?></p>
    
    <?php
    // Conditional logic
    $hour = date("H");
    if ($hour < 12) {
        echo "<p>Good Morning!</p>";
    } else {
        echo "<p>Good Afternoon!</p>";
    }
    ?>
</body>
</html>

<?php
// ============================================
// COMMON SYNTAX MISTAKES TO AVOID
// ============================================

// MISTAKE 1: Missing semicolon
// $name = "John"     // ERROR: missing semicolon
$name = "John";        // CORRECT

// MISTAKE 2: Forgetting $ for variables
// name = "John";      // ERROR: missing $
$name = "John";        // CORRECT

// MISTAKE 3: Using $ with constants
// define("SITE", "MySite");
// echo $SITE;         // ERROR: constants don't use $
echo SITE;             // CORRECT

// MISTAKE 4: Nested PHP tags
<?php
    // <?php echo "test"; ?>  // ERROR: can't nest tags
    echo "test";                // CORRECT
?>

// MISTAKE 5: Extra whitespace after closing tag
<?php
echo "Hello";
?> 
<!-- Extra whitespace after ?> can cause "headers already sent" errors -->

// ============================================
// BEST PRACTICES
// ============================================

/*
1. ALWAYS use <?php ?> tags (not short tags)
2. OMIT closing tag in pure PHP files
3. END statements with semicolon
4. USE consistent indentation (2 or 4 spaces)
5. ADD comments for complex logic
6. BE CONSISTENT with case (lowercase for keywords)
7. AVOID mixing PHP and HTML unnecessarily
8. USE meaningful variable names
9. KEEP code organized and readable
*/

// Good Practice Example
<?php
// Initialize variables
$userName = "John Doe";
$userAge = 30;

// Process data
$message = "Welcome, " . $userName;

// Output result
echo $message;
// No closing tag

// ============================================
// QUICK REFERENCE SUMMARY
// ============================================

/*
PHP SYNTAX BASICS:
-----------------
Statements end with ;      → echo "Hi";
Variables start with $     → $name = "John";
Case-sensitive variables   → $name ≠ $Name
Case-insensitive keywords → echo = ECHO
Comments: //, #, /* */
Whitespace ignored        → spaces, tabs, newlines

PHP TAGS:
---------
Standard:   <?php ... ?>   (ALWAYS USE THIS)
Short:      <? ... ?>      (NOT recommended)
ASP:        <% ... %>      (DEPRECATED)
Script:     <script>       (RARE)

KEY RULES:
---------
1. Opening tag <?php is MANDATORY
2. Closing tag ?> is OPTIONAL
3. Pure PHP files → NO closing tag
4. Outside tags = HTML
5. Inside tags = PHP code
*/
?>